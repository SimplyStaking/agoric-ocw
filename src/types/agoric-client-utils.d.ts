declare module '@agoric/client-utils' {
  // Declare any types or interfaces for the module here.
  // For now, you can use 'any' to bypass the error, and then later replace it with more specific types.

  export function makeVstorageKit(...args: any[]): any;
  export function boardSlottingMarshaller(...args: any[]): any;
}